services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:8080
    depends_on:
      - api

  api:
    build: ./api
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      CORS_ORIGIN: http://localhost:5173
